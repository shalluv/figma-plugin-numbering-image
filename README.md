# Numbering Image

If you ever need to number your images, but doing it manually is too time-consuming, then this tool is perfect for you.

## Usage

If your image names are in the format `image x`, where `x` is a number, this plugin will create a text instance with the number `x` and position it in the top left corner of the image.